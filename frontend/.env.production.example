# Production build config. Copy to `.env.production` and fill in.
# Vite picks this up automatically on `vite build` (mode "production").
# VITE_ vars are baked into the client bundle at build time — public, not secret.

VITE_API_URL="https://api.example.com"
